import { SidebarLayout } from "./sidebarLayout";


/**
 * Layout namespace aggregates different layout components.
 * Usage:
 *   <Layout.Sidebar> ... </Layout.Sidebar>
 */
const Layout = {
  Sidebar: SidebarLayout,
};

export default Layout;
